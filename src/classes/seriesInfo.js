class SeriesInfo {
  constructor(idImdb, genres, plot, rate, rateCount) {
    this.idImdb = idImdb;
    this.genres = genres;
    this.plot = plot;
    this.rate = rate;
    this.rateCount = rateCount;
  }
}

module.exports = SeriesInfo;
